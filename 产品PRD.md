# 收机参谋 · 产品逻辑 PRD（v1.2）

> 产品定位：面向二手 3C 卖家（尤其华强北背包客 / 代采）的「实时行情看板 + 收货决策助手」。核心价值 = 帮人短时间聚合实时行情信息（人做不到，AI 能做），辅助决策而非替代决策。差异化：转转帮有货的人估价出手，我帮收货的人做行情参考与决策避坑。

## 一、全链路

行情数据三通道（回收平台 / 闲鱼 / 成交回写）→ 归一化清洗 → 行情看板（实时聚合）→ Agent 决策（字段解析 → 风险扫描 → 估价倒推 → 五维评分 → 案例召回）→ 收货卡 → 成交回写 → 校准反哺。

## 二、数据采集（三通道）

| 通道 | 来源 | 怎么拿 | 作用 |
|---|---|---|---|
| A 地板价 | 回收平台估价 / 官方换购 | 公开估价接口（选机型成色出价） | 出手价下限锚点 |
| B 市场价 | 闲鱼公开搜索页 | 关键词轮询 + 限频 + 去重 + 失败降级 | 挂价 / 需求热度 / 卖家信用 |
| C 成交价 | 用户回写 | 成交后填实际卖价 | EMA 校准 |

合规：只读公开数据、遵守 robots、限频、不抓隐私、不自动回复/下单、失败降级不影响主流程。

## 三、输入与交互

| 方式 | 状态 | 说明 |
|---|---|---|
| 行情看板 | ✅ | 默认页：搜索 + 版本/存储筛选 + 点击机型看详情 |
| 对话（打字） | ✅ | Agent 追问填槽 |
| 语音 | ✅ | 浏览器 Web Speech |
| 截图 | ⏳ 占位 | 需 OCR / 多模态（DeepSeek 非多模态） |
| 批量 | ⏳ 规划 | 粘贴多条 / 上传 JSON/CSV → 清单视图 |

## 四、Agent 架构

三个 Agent：对话 Agent（追问/跑题拉回）、解析 Agent（LLM Function Calling + 规则双保险）、评估 Agent（五维 + 报价 + 红线 + RAG）。

状态机：空槽开场 → 解析合并槽位 → 缺则追问（≤3 项）→ 齐则评估出卡 → 记入历史 → 成交回写改状态。

## 五、提示词（完整版见 functions/api/chat.js）

系统提示词要点：懂行的收货老搭档口吻 + 行业黑话（靓机/小花/卡贴机/官换机）+ 六条铁律（不复读、数字歧义先问、追问≤3项、纠错顺着改、跑题拉回、客观）+ 边界（不承诺100%、不能实时查价、不做法律/财务建议）。输出 JSON {parsed, reply}。

## 六、变量字典

- 字段：model / storage / region / carrier_lock / condition_grade / battery_health / screen_state / repair_history / seller_type / listed_price 等 30+ 字段
- 风险 9 类，红线 3 类（ID锁/隐藏ID、监管机、翻新机）→ 直接不收
- 五维：价差空间30 / 品相风险25 / 流动性20 / 卖家可信度10 / 行情窗口15
- 决策：verdict ∈ {buy 收, watch 观望, skip 不收}；confidence ∈ {high, medium, low}

## 七、RAG 与锚点

- 知识库：静态知识 19 条（行情基线 / 验机通病 / 机型参数 / 问卖家话术）+ 复盘案例 10 条（可增长）
- 锚点：19 条，覆盖 16 款 iPhone 机型；按机型/存储/风险词召回 top5
- 校准：新锚点 = 旧锚点 + 0.2×(实际成交 − 旧锚点)；MAE/MAPE/bias 监控

## 八、还差什么

已完成：行情看板、文本解析、风险识别、估价倒推、收货卡、Agent 对话、历史记录、成交回写、基础 RAG。

还差（按优先级）：① 真实行情数据接入 ② 批量评估 + 清单 ③ 截图 OCR / 多模态 ④ 向量化 RAG（Chroma）⑤ 账本导入 ⑥ 复盘看板 ⑦ 效果评估（专家对照一致率）。

## 九、API 与安全

DeepSeek Key 只在服务端（EdgeOne 环境变量 DEEPSEEK_API_KEY / .env），前端永不出现。公开 demo 规则版无 Key；AI 版走 /api/chat 边缘函数。
